apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-klusterapp-ingress
spec:
  ingressClassName: traefik
  rules:
  - host: demo.klusterapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: klusterapp-svc
            port:
              number: 80